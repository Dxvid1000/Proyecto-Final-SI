
  <body>
    <form action="/borrar" method="POST">
      <div class="container">
        {{#each grupos}}
        <table class="table">
          <thead>
            <tr>
              <th>Nombre del grupo</th>
              <th>Numero de integrantes</th>
              <th>Genero</th>
              <th>Nacionalidad</th>
              <th>Año de Creación</th>
              <th>Año de separación</th>
              <th>Borrar</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>{{nombreGrupo}}
                  	<input type="text" class="form-control" id="nombreGrupo" value="{{nombreGrupo}}" name="nombreGrupo">
              </td>
              <td>{{numIntegrantes}}
                    <input type="text" class="form-control" id="numIntegrantes" value="{{numIntegrantes}}" name="numIntegrantes">

              </td>
              <td>{{genero}}
                   	<input type="text" class="form-control" id="genero" value="{{genero}}" name="genero">

              </td>
              <td>{{nacionalidad}}
                  <input type="text" class="form-control" id="nacionalidad" value="{{nacionalidad}}" name="nacionalidad">
              </td>
                	

              <td>{{anioCreacion}}
                  <input type="text" class="form-control" id="anioCreacion" value="{{anioCreacion}}" name="anioCreacion">

              </td>

              <td>{{anioSeparacion}}
                   <input type="hidden" class="form-control" id="anioSeparacion" value="{{anioSeparacion}}" name="anioSeparacion">

              </td>
              <td><button type="submit" class="btn btn-default">Borrar Grupo</button></td>
            </tr>
          </tbody>
        </table>
         {{/each}}
      </div>
    </form>
  </body>
